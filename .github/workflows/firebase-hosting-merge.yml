# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create .env file for build
        run: |
          cat > .env << EOF
          NEXT_PUBLIC_FIREBASE_API_KEY=${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID=${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
          FIREBASE_CLIENT_EMAIL=${{ secrets.FIREBASE_CLIENT_EMAIL }}
          FIREBASE_PRIVATE_KEY=${{ secrets.FIREBASE_PRIVATE_KEY }}
          EOF
      - name: Update firebase.json with secrets for Cloud Build
        run: |
          sed -i 's/API_KEY_PLACEHOLDER/${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}/g' firebase.json
          sed -i 's/AUTH_DOMAIN_PLACEHOLDER/${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}/g' firebase.json
          sed -i 's/PROJECT_ID_PLACEHOLDER/${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}/g' firebase.json
          sed -i 's/STORAGE_BUCKET_PLACEHOLDER/${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}/g' firebase.json
          sed -i 's/MESSAGING_SENDER_ID_PLACEHOLDER/${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}/g' firebase.json
          sed -i 's/APP_ID_PLACEHOLDER/${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}/g' firebase.json
          sed -i 's/CLIENT_EMAIL_PLACEHOLDER/${{ secrets.FIREBASE_CLIENT_EMAIL }}/g' firebase.json
          sed -i 's/PRIVATE_KEY_PLACEHOLDER/${{ secrets.FIREBASE_PRIVATE_KEY }}/g' firebase.json
      - name: Install dotenv and build
        run: |
          npm install -g dotenv
          dotenv -e .env npm ci && dotenv -e .env npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DRIP_DROP_2C2C8 }}
          channelId: live
          projectId: drip-drop-2c2c8
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
